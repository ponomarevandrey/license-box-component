mixin embed(url, classList='embed')
  div(class=`${classList}`) 
    iframe(
      src=`${url}`
      frameborder='0' 
      allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
    )
